@inherits LayoutComponentBase

<MudThemeProvider Theme="NetChan" />

<MudLayout>
	<MudMainContent>
		@Body
	</MudMainContent>
</MudLayout>

@code {

	// Frontpage layout.

	public static MudTheme NetChan = new MudTheme() {
		Palette = new Palette() {
			Primary = "#00adef",
			Secondary = "#eefaff",
			Background = Colors.Blue.Lighten5,
			Error = "#d00",
			Success = "#789922"
		},

		LayoutProperties = new LayoutProperties() {
			DefaultBorderRadius = "0",
			AppbarMinHeight = "20px"
		}
	};
}